function [Rr, Rc, cross] = adaptice_cross_approx(A,n,m, r)
    Rr = zeros(n, r); %R rows
    Rc = zeros(m, r); %R columns (stored as rows)
    is = zeors(m, r); js = zeros(n, 1);
    Remainder = zeros(n,m);
    for k=1:r
        
        
    end

end